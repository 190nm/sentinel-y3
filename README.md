# sentinel

### Set-up

**Note:** Requires `libgk.so` which can be found under `/lib/x86/libgk.so` in some older versions but should be sourced from `v.1.9.0` if possible. Extract to: `sentinel/data/`